This is the mpi version of Qnorm.

- Run in not cluster system: 

  You need the mpi package, e.g. on Debian:

    apt-get install mpich-bin lam-4-dev liblam4 lam-runtime

  And edit the hostnames with the number of processor that you want emulate, and run mpi

    lamboot hostnames
   
- Run in cluster system:

  You need to kown that mpi is installed.


compile with

  make all
	make test

